/*
We need a function that can transform a number into a string.

Examples:
123 --> "123"
999 --> "999"
*/

//Here the method toString() is taking in the parameter num which would be input by the user and converting it to a string.
function numberToString(num) {
return num.toString();
}
